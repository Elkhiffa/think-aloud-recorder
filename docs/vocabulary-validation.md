# 词库与回看跟进验证（2026-09-22）

- 162 项 Python 测试通过，覆盖真实词表、默认快照、新预设与已有预设的隔离、
  显式清空和打包白名单。加入词库说明的打包项后，17 项打包检查再次通过。
- 29 项 Node 状态与回看计算测试通过；Edge 中 32 项界面检查通过，包含默认选用、
  146 词预览、浅色/深色、紧凑窗口滚动及取消不保存，生成 29 张截图。
- 隐藏原生 WebView2 的 6 项 DOM 交互检查通过：默认选用、预览、滚动、返回、
  移除与重建草稿。使用真实产品页面和内置词表，桥接状态为合成数据。
- 回看源比例另有 10 项真实 Edge 检查，见 `review-aspect-validation.md`。

浏览器首轮发现两项旧断言依赖固定栏宽。自动适配模式增加了 320 px 的逐字稿可读宽度，
并改用向左移动分隔条验证手动调整，避免已在上限时要求继续右移。再次运行 32 项全通过。
原始失败报告保留于 ignored `work/vocabulary-acceptance`，通过报告位于
`work/vocabulary-acceptance-final`；没有丢弃失败证据或把旧断言直接绕过。

上述验证未录制麦克风、调用 OBS 或上传云端音频，不代表真实转写准确率已测量。

私有试用包 `0.5.1-preview.3` 已生成，保留既有二进制公开发行门槛。更新包从 preview.2
升级到 preview.3，共 428889 字节；应用后全部 7031 个目标文件的 SHA256 与完整包一致。
更新包中没有配置、密钥、模型、录制或个人词库。打包源码与当前 48 个本地对应文件一致，
包内运行时加载的产品模块路径及 146 词文件均验证通过；完整包生成的版本元数据单独核对。

本地试用安装仅补入 `vocabularies/uiux-terms.txt`，与仓库文件字节一致。新的程序行为通过
覆盖更新包生效，未改写现有预设或运行中的应用。
